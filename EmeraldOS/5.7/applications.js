/* =========================================================
   EMERALDOS 5.7 - APPLICATION NOTES
   The built-in application registry is in registry.js.
   The active application renderers are imported by os.js.

   Keep this file for compatibility with earlier EmeraldOS builds that
   expected a separate applications.js file.
========================================================= */
export { SYSTEM_APPS, getApp } from "./registry.js";
