VITE_BACKEND_URL=http://localhost:3001

# URL of the robot-voice TTS service (separate Render deployment from the
# main backend above). Leave unset to fall back to the browser's built-in
# voice -- useful for local dev without the TTS service running.
VITE_TTS_URL=